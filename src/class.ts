export interface obj{
name:string;
email:string;
password:string;
cateogry:string;
confirmPassword:string;
}
export default arr;
var arr = new Array();

export interface select{
    category:string;
    name:string;
    url;
}